#User function Template for python3

class Solution:
    
    def getSingle(self,arr):
        # code here
        dic={}
        for i in arr:
            if i not in dic:
                dic[i]=1
            else:
                dic[i]+=1
        for i in dic:
            if dic[i]%2!=0:
                return i
